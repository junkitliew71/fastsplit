# Receipt regression dataset

Each JSON manifest records human-reviewed expected items and summary fields. Receipt images belong in the gitignored `samples/` directory and are never committed by default. Add a sample only with the receipt owner's permission, then evaluate available local samples with `evaluate_dataset.py`.
