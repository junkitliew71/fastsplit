import {memo,useCallback,useEffect,useMemo,useRef,useState}from'react';
import type{OcrToken}from'../types';
const Token=memo(function Token({token,imageWidth,imageHeight,selected,assigned,onTap,debug}:{token:OcrToken;imageWidth:number;imageHeight:number;selected:boolean;assigned:boolean;onTap:(id:string)=>void;debug:boolean}){return <button draggable onDragStart={e=>{e.dataTransfer.effectAllowed='copy';e.dataTransfer.setData('text/plain',token.id);}} className={`ocr-box ${selected?'selected':''} ${assigned?'assigned':''} ${token.confidence<.7?'low':''}`} style={{left:`${token.bbox.x/imageWidth*100}%`,top:`${token.bbox.y/imageHeight*100}%`,width:`${token.bbox.width/imageWidth*100}%`,height:`${token.bbox.height/imageHeight*100}%`}} onClick={()=>onTap(token.id)} title={`${token.text} · ${Math.round(token.confidence*100)}% · x:${token.bbox.x} y:${token.bbox.y} w:${token.bbox.width} h:${token.bbox.height}`}>{debug&&<span>{token.text} {Math.round(token.confidence*100)}%</span>}</button>});
export function ReceiptViewer({imageUrl,imageWidth,imageHeight,tokens,selected,assigned,onTokenTap}:{imageUrl:string;imageWidth:number;imageHeight:number;tokens:OcrToken[];selected:string[];assigned:Set<string>;onTokenTap:(id:string)=>void}){const layer=useRef<HTMLDivElement>(null),debug=new URLSearchParams(location.search).get('ocrDebug')==='1',[rendered,setRendered]=useState({width:0,height:0});useEffect(()=>{if(!debug||!layer.current)return;const observer=new ResizeObserver(([entry])=>setRendered({width:Math.round(entry.contentRect.width),height:Math.round(entry.contentRect.height)}));observer.observe(layer.current);return()=>observer.disconnect();},[debug]);const clean=useMemo(()=>tokens.filter((token,index,list)=>token.text.trim()&&token.confidence>=.2&&token.bbox.width<imageWidth*.9&&token.bbox.height<imageHeight*.25&&!list.slice(0,index).some(other=>other.text===token.text&&Math.abs(other.bbox.x-token.bbox.x)<3&&Math.abs(other.bbox.y-token.bbox.y)<3)),[tokens,imageWidth,imageHeight]),tap=useCallback((id:string)=>onTokenTap(id),[onTokenTap]);return <div className="receipt-pan receipt-drag-source"><div ref={layer} className="receipt-transform-layer" style={{aspectRatio:`${imageWidth}/${imageHeight}`}}><img draggable={false} src={imageUrl} alt="Your receipt"/>{clean.map(t=><Token key={t.id} token={t} imageWidth={imageWidth} imageHeight={imageHeight} selected={selected.includes(t.id)} assigned={assigned.has(t.id)} onTap={tap} debug={debug}/>)}</div>{debug&&<div className="ocr-coordinate-debug">OCR {imageWidth}×{imageHeight} · Rendered {rendered.width}×{rendered.height} · Scale {(rendered.width/imageWidth||0).toFixed(3)}×{(rendered.height/imageHeight||0).toFixed(3)}</div>}</div>}
